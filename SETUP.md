# 🚀 Установка и запуск проекта

## Требования

- Node.js 16+ (рекомендуется 18+)
- npm или yarn
- Git

## Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск в режиме разработки

```bash
npm run dev
```

Проект будет доступен на: `http://localhost:3000`

### 3. Сборка для продакшена

```bash
npm run build
npm start
```

## 📁 Структура проекта

```
lord/
├── src/
│   ├── app/                  # Next.js App Router страницы
│   │   ├── page.tsx         # Главная страница
│   │   ├── layout.tsx       # Root layout
│   │   ├── catalog/         # Каталог товаров
│   │   │   └── page.tsx
│   │   ├── cart/            # Корзина и checkout
│   │   │   └── page.tsx
│   │   ├── contacts/        # Контакты
│   │   │   └── page.tsx
│   │   ├── delivery/        # Информация о доставке
│   │   │   └── page.tsx
│   │   ├── promo/           # Акции
│   │   │   └── page.tsx
│   │   ├── profile/         # Личный кабинет
│   │   │   └── page.tsx
│   │   └── admin/           # Админ-панель
│   │       └── page.tsx
│   ├── components/           # React компоненты
│   │   ├── ui/              # UI компоненты
│   │   │   ├── Button.tsx
│   │   │   ├── ProductCard.tsx
│   │   │   ├── CartPopup.tsx
│   │   │   └── index.ts
│   │   ├── layout/          # Layout компоненты
│   │   │   ├── Navigation.tsx
│   │   │   ├── Footer.tsx
│   │   │   ├── Layout.tsx
│   │   │   └── index.ts
│   │   └── index.ts
│   ├── store/               # State management
│   │   └── cartStore.ts     # Zustand store для корзины
│   ├── data/                # Статические данные
│   │   └── products.ts      # Каталог товаров и компания данные
│   ├── types/               # TypeScript типы
│   │   └── index.ts
│   └── globals.css          # Глобальные стили
├── public/                  # Статические файлы
├── package.json
├── tsconfig.json
├── tailwind.config.ts
├── next.config.js
└── postcss.config.js
```

## 🛠️ Технические детали

### Используемые библиотеки

- **Next.js 14+** - React фреймворк с SSR и SSG
- **TypeScript** - Типизированный JavaScript
- **Tailwind CSS** - Utility-first CSS фреймворк
- **Zustand** - Лёгкий state management
- **clsx** - Утилита для работы с классами

### Основные компоненты

#### Button
Переиспользуемая кнопка с разными вариантами:
```tsx
<Button variant="primary" | "secondary" | "outline" size="sm" | "md" | "lg">
  Текст
</Button>
```

#### ProductCard
Карточка товара с информацией и кнопкой добавления:
```tsx
<ProductCard product={product} onAddToCart={() => {}} />
```

#### CartPopup
Всплывающее окно корзины:
```tsx
<CartPopup isOpen={true} items={items} onAddToCart={() => {}} />
```

### State Management (Zustand)

Корзина управляется через Zustand store:

```typescript
import { useCartStore } from '@/store/cartStore';

// В компоненте:
const { items, addItem, removeItem, updateQuantity, getTotalPrice } = useCartStore();
```

**Доступные методы:**
- `addItem(product, quantity)` - добавить товар
- `removeItem(productId)` - удалить товар
- `updateQuantity(productId, quantity)` - изменить количество
- `clearCart()` - очистить корзину
- `getTotalPrice()` - получить сумму
- `getTotalItems()` - получить количество товаров

## 🎨 Дизайн система

### Цвета

```css
primary-white: #FFFFFF
primary-red: #FF0000
primary-dark: #1A1A1A
```

### Типография

Шрифты загружаются через Google Fonts:
- **Inter** - основной шрифт интерфейса
- **Montserrat** - альтернативный sans-serif
- **Playfair Display** - декоративный шрифт для заголовков

## 📱 Адаптивность

Проект полностью адаптирован для всех размеров экрана:

```
Mobile (320px+)  - сжатый интерфейс
Tablet (768px+)  - 2-колончный layout
Desktop (1024px+) - полный 3-колончный layout
```

## 🔄 Основные страницы и функции

### Главная (`/`)
- Hero-секция с призывом к действию
- Категории товаров
- Преимущества компании
- CTA блоки

### Каталог (`/catalog`)
- Список всех товаров
- Фильтрация по категориям
- Поиск
- Сортировка (по цене, рейтингу)
- Карточки товаров с кнопкой добавления в корзину

### Корзина (`/cart`)
- Просмотр товаров в корзине
- Изменение количества
- Удаление товаров
- Многошаговый checkout:
  1. Контактная информация
  2. Адрес и детали доставки
  3. Выбор способа оплаты
  4. Подтверждение заказа

### Доставка (`/delivery`)
- Информация о сроках доставки
- Стоимость доставки
- Зоны доставки
- Способы оплаты

### Контакты (`/contacts`)
- Адреса и телефоны
- Форма обратной связи
- FAQ
- Карта (placeholder)

### Профиль (`/profile`)
- Вход/регистрация
- История заказов
- Сохраненные адреса
- Редактирование профиля

### Акции (`/promo`)
- Действующие скидки
- Комбо-предложения
- Условия акций

### Админ-панель (`/admin`)
- Управление товарами (CRUD)
- Просмотр заказов
- Модерация отзывов

## 📊 Данные товаров

Все товары хранятся в `/src/data/products.ts`:

```typescript
import { products, categories, companyInfo } from '@/data/products';
```

### Структура Product:
```typescript
{
  id: string
  name: string
  category: 'perepechis' | 'tabanis' | 'pizza' | 'drinks'
  price: number
  weight: string
  description?: string
  image?: string
  rating?: number
  reviews?: number
}
```

## 🔐 Безопасность

- ✅ TypeScript типизация
- ✅ Валидация форм на клиенте
- ✅ React защита от XSS
- ✅ HTTPS-ready конфигурация

## 🚀 Деплой

### Вариант 1: Vercel (рекомендуется для Next.js)

```bash
npm i -g vercel
vercel
```

### Вариант 2: Docker

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
CMD ["npm", "start"]
```

### Вариант 3: Любой Node.js хостинг

```bash
npm run build
npm start
```

## 🐛 Отладка

### Включить режим debug:

```bash
DEBUG=next:* npm run dev
```

### Проверить типы:

```bash
npm run type-check
```

## 📝 Важные команды

```bash
npm install           # Установить зависимости
npm run dev          # Разработка
npm run build        # Сборка
npm start            # Продакшен
npm run type-check   # Проверка типов
npm run lint         # Linting
```

## 🤝 Контрибьютинг

Для добавления новых функций:

1. Создайте ветку: `git checkout -b feature/название`
2. Коммитьте изменения: `git commit -m 'описание'`
3. Пушьте: `git push origin feature/название`
4. Создайте Pull Request

## 📞 Поддержка

Для вопросов и проблем:
- 📞 Телефон: 8 (912) 874-61-07
- 📧 Email: поддержка по контактам на сайте

---

**Успешного развитие! 🚀**
